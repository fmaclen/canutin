services:
  pocketbase:
    image: ghcr.io/fmaclen/canutin:next
    working_dir: /app/pocketbase
    command: ['./pocketbase-custom', 'serve', '--http', '0.0.0.0:42070']
    ports:
      - '42070:42070'
    environment:
      DEMO_ENABLED: 'true'
      DEMO_EMAIL: 'demo@canutin.com'
      DEMO_PASSWORD: '123qweasdzxc'
    volumes:
      - pocketbase-data:/app/pocketbase/pb_data
    restart: unless-stopped

  sveltekit:
    image: ghcr.io/fmaclen/canutin:next
    command: ['node', 'build/index.js']
    ports:
      - '42069:42069'
    environment:
      PUBLIC_PB_URL: 'https://pb-next.canutin.com'
      PUBLIC_DEMO_ENABLED: 'true'
      PUBLIC_DEMO_EMAIL: 'demo@canutin.com'
      PUBLIC_DEMO_PASSWORD: '123qweasdzxc'
      ORIGIN: 'https://next.canutin.com'
    depends_on:
      - pocketbase
    restart: unless-stopped

volumes:
  pocketbase-data:
